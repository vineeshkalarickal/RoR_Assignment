<div class="pull-right">
<%=  %>
  <%= link_to cart_path do %>
  <span class="btn btn-mini btn-primary">
    <i class="icon-shopping-cart icon-white"></i>
    [ <%= LineItem.all.where(:user_id => current_user.id).count %> ] Itemes in your cart
  </span>
  <% end %>
</div>
